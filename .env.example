# Chave de API para autenticação do serviço
API_KEY=sua_chave_api_aqui

# Origens permitidas para CORS (separadas por vírgula)
# Use * para permitir todas as origens ou especifique domínios
# Exemplo: https://seu-dominio.com,https://outro-dominio.com
CORS_ALLOW_ORIGINS=*

# Porta em que o servidor irá rodar (padrão: 5000)
PORT=5000

# Tamanho máximo do arquivo em bytes (padrão: 20MB = 20971520 bytes)
MAX_FILE_SIZE=20971520

# Diretório para arquivos temporários
UPLOAD_FOLDER=/tmp/uploads

# Nível de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Configurações de ambiente
FLASK_ENV=production        # production ou development
FLASK_DEBUG=0              # 1 para debug, 0 para produção